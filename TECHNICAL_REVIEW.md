# Technical review

Local technical review is clean; the core walker rejects missing, non-directory, and symlink roots, skips symlink files and does not traverse symlink directories; direct hash inputs normalize missing and non-directory-parent diagnostics, covered by `test_sha256_file_rejects_missing_input`, `test_sha256_file_rejects_non_directory_parent`, `test_hash_tree_rejects_invalid_roots`, `test_hash_tree_rejects_symlink_root`, `test_hash_tree_skips_symlink_files`, and `test_hash_tree_does_not_traverse_symlink_directories`; direct `sha256_file` calls also reject symlinks and symlinked parent directories, covered by `test_sha256_file_rejects_symlink` and `test_sha256_file_rejects_symlink_parent`; non-regular inputs are rejected as `refusing non-regular hash input`, and hashing requires an `O_NOFOLLOW` descriptor and fails closed when unavailable, with pre/post device, inode, size, and modification-time checks plus a no-follow pathname device/inode check, covered by `test_sha256_file_rejects_non_regular_input`, `test_sha256_file_rejects_missing_no_follow`, `test_sha256_file_rejects_metadata_change`, and `test_sha256_file_rejects_path_identity_change`; `_check_resources` rejects zero free bytes and inode exhaustion before migration work, covered by `test_check_resources_rejects_no_free_bytes` and `test_check_resources_rejects_inode_exhaustion`; exactly two local gates are checked and intentionally separate from the nine unchecked production checks; passing local `make review` is not production approval, guarded by `test_review_separates_local_and_production_status`; see [PROGRESS.md](PROGRESS.md) for the iteration history; if that link is stale, restore `PROGRESS.md` and rerun the focused CLI test; progress-link parity is asserted by `test_readme_names_documented_status_test` in `tests/test_cli.py`; production Oxygen/Synology acceptance remains pending because the environment is unavailable; `test_review_keeps_live_acceptance_pending` prevents local CI from implying those checks passed, covering migration, metadata, ledger, and backup acceptance items; it also requires all nine production checklist entries to remain unchecked, including external `.objects` placement and explicit `--root` GC lock coordination; the test names object/hardlink counts, migration, indexing, WebDAV, metadata, ledger, and backup checks individually. Focused transport checks can be reproduced with `uv run pytest tests/test_remote.py tests/test_audit.py tests/test_cli.py`; CLI diagnostic examples are mocked local validation, not live Oxygen acceptance; malformed-output checks remain local-only until live Oxygen acceptance; the CLI module also verifies linked review and assumptions files exist via `test_documented_status_files_exist`, whose name is documented in the README; the CLI example uses the stable `photostow failed: remote discovery output is not UTF-8` diagnostic; transport failures require correction before rerunning, and unchanged input is not retried automatically; [README retry guidance](README.md) and technical review share the `before rerunning` action boundary, covered by the parity assertions in `tests/test_cli.py`; stale README/review names fail the focused CLI test rather than silently drifting; remediation updates `README.md`, `TECHNICAL_REVIEW.md`, and [`tests/test_cli.py`](tests/test_cli.py) together; stale reciprocal navigation fails the focused CLI test; `PROGRESS.md` iteration 473 records that parity coverage in `tests/test_cli.py`.

## Verified locally

- [x] `make review` passes: tests, Ruff, formatting, mypy, lock consistency, and diff checks.

Re-run the local gate with:

```sh
make review
```
- Migration defaults to dry-run and uses bounded progress, manifests, locking, resource checks, no-follow hashing, and object verification. `--limit` is applied after discovery, so it bounds processing but does not make a large directory walk cheap; use `--path` for a genuinely small trial subtree.
- Apply is manifest-authoritative and safely rerunnable.
- Ingest refuses conflicting destinations and publishes verified hardlinks atomically.
- Verify and report-only GC reject malformed/corrupt object stores.
- Dry-run manifests are atomically replaced, then both the manifest file and its parent directory are fsynced.
- A failed manifest replacement preserves the prior manifest and cleans the temporary file.
- Migration revalidates source content immediately before replacing a visible path.
- The final portable `os.replace` still has a small TOCTOU window; repeated mutation tests cover the mitigation, not atomic compare-and-swap.
- Remote review-stage tar manifests include only regular non-symlink files; directories, FIFOs, sockets, and other non-regular entries are excluded.
- Remote chmod traversal includes only regular non-symlink year directories.
- Both remote tar-copy paths and remote hashing use NUL-delimited path transport; tar copies preserve newlines, while hash updates reject newline paths because the ledger is line-based; this capability difference is intentional and documented. Empty hash input invokes no subprocess. Hash output shares the documented UTF-8 filename assumption; malformed hash output is rejected with `remote hash output is not UTF-8`, covered by `test_remote_sha256_rejects_malformed_utf8`; relative paths outside the source root are rejected as `path is outside source root`, covered by `test_relative_paths_rejects_outside_root`; missing TSV rows with the wrong column count are rejected as `missing TSV row must have four columns`, covered by `test_missing_records_rejects_short_row`; adjusted flags must be `0` or `1`, covered by `test_missing_records_rejects_invalid_adjusted_flag`; the complete pipeline is locally covered: NUL input, `sha256sum --zero` output, multi-record normalization, and strict 64-hex-digest/path validation produce one ledger line per file; malformed output and newline paths are rejected rather than corrupting the ledger. Local GNU coreutils supports `sha256sum --zero` for newline-containing names, while Synology support remains an acceptance item.
- Direct remote copies reject symlink source paths before starting subprocesses.
- Direct remote copies require relative source paths to resolve beneath `source_root`, blocking absolute and symlinked-parent traversal.
- `copy-missing` rejects missing-record paths outside `source_root` before dry-run output or transfer.
- Laptop CLI `ValueError`, `UnicodeDecodeError`, and `OSError` failures, and filesystem failures return exit 1 with concise stderr diagnostics; README wording is guarded by `tests/test_cli.py`, including `test_main_reports_validation_failure` and `test_main_reports_os_error`, which preserves the complete `photostow failed: [Errno 13] permission denied` line; README, review, and test-name parity are checked together by `test_readme_names_documented_status_test`, including the complete `[Errno 13]` line; the README exception-class phrase is part of that guard. README’s `[Errno 13]` detail is guarded by `test_readme_names_documented_status_test`. `test_main_reports_decode_failure` checks the `photostow failed:` prefix, codec, byte position, and decode message; README and review retain the same detail wording; Photos DB asset paths are contained beneath `originals`, covered by `test_iter_assets_rejects_path_outside_originals`; SQLite read-only URIs use encoded paths, covered by `test_iter_assets_opens_special_character_library_path`; empty directory and filename components are rejected, covered by `test_iter_assets_rejects_empty_path_components`; non-finite Apple timestamps are rejected as `Apple timestamp must be finite`, covered by `test_apple_timestamp_rejects_non_finite`; non-numeric values are rejected as `Apple timestamp must be numeric`, covered by `test_apple_timestamp_rejects_non_numeric`; finite out-of-range values are rejected as `Apple timestamp is out of range`, covered by `test_apple_timestamp_rejects_out_of_range`; both state that Production Oxygen/Synology acceptance remains pending because the environment is unavailable; `tests/test_cli.py` reads documentation as explicit UTF-8 to avoid locale-dependent parity results. `copy-missing --dry-run` validates resolved sources before printing paths.
- Shared direct-copy validation requires relative paths, resolved containment, and non-symlink sources.
- Audit NUL-delimited stat records intentionally preserve newline filenames, covered by `test_remote_files_preserves_newline_filenames`, with the boundary recorded in `PROGRESS.md`; both remote discovery scanners use NUL-delimited path records; `remote_find` and `audit.remote_files` reject results outside their requested roots; a shared `remote_path_beneath` helper requires a strict normalized descendant, rejects relative sibling escapes after dot-segment normalization, including `/.` root spellings, and canonicalizes leading slashes on both root and candidate paths, and rejects empty, whitespace-only, or NUL-bearing inputs and trailing-slash prefixes that prevent sibling-prefix matches, including filesystem-root, absolute-root, relative-root, current-directory (including `.` and `./` spellings), and repeated-slash edge cases; current-directory scanner output, including `./` candidate paths, is canonicalized; both scanners reject empty, whitespace-only, or NUL-containing roots before shell execution, preventing sibling names such as `/photo2` or dot-dot paths from matching `/photo`; canonical paths from both discovery and audit scanners are deduplicated before hashing or duplicate analysis; identical same-size audit records are covered by `test_remote_files_deduplicates_same_size_paths`; empty duplicate reports produce no groups, covered by `test_parse_duplicate_group_file_ignores_empty_report`; groups with fewer than two paths are rejected as `duplicate report group must have two paths` during both serialization and parsing, and repeated paths are rejected as `duplicate report group paths must be unique`, while paths repeated across groups are rejected as `duplicate report path appears in multiple groups`, covered by `test_write_duplicate_groups_rejects_repeated_path`, `test_parse_duplicate_group_file_rejects_repeated_path`, `test_write_duplicate_groups_rejects_cross_group_path`, and `test_parse_duplicate_group_file_rejects_cross_group_path`; parsed empty or whitespace-only paths are rejected as `duplicate report path must not be empty`, covered by `test_write_duplicate_groups_rejects_short_group`, `test_parse_duplicate_group_file_rejects_singleton`, and `test_parse_duplicate_group_file_rejects_empty_path`; duplicate report serialization rejects non-file destinations, missing parents, symlink outputs, and any symlinked parent before writing; parsing rejects missing, non-file, symlink, and symlinked-parent inputs before reading, covered by `test_parse_duplicate_group_file_rejects_directory_input`, `test_parse_duplicate_group_file_rejects_missing_input`, `test_parse_duplicate_group_file_rejects_symlink_parent`, `test_parse_duplicate_group_file_rejects_symlink`, `test_write_duplicate_groups_rejects_missing_parent`, `test_write_duplicate_groups_rejects_directory_destination`, `test_write_duplicate_groups_rejects_symlink_output`, `test_write_duplicate_groups_rejects_symlink_parent`, and `test_write_duplicate_groups_rejects_nested_symlink_parent`; it validates all paths before writing; it rejects empty or whitespace-only paths as `duplicate report path must not be empty` and newline-, carriage-return-, or NUL-bearing paths as `duplicate report path contains unsupported newline` or `duplicate report path must not contain NUL`; parsing preserves CR bytes before splitting and normalizes malformed UTF-8 to `duplicate report is not valid UTF-8`, covered by `test_write_duplicate_groups_validates_before_writing`, `test_write_duplicate_groups_rejects_empty_path`, `test_write_duplicate_groups_rejects_whitespace_path`, and `test_write_duplicate_groups_rejects_line_terminator`, `test_write_duplicate_groups_rejects_newline_path`, `test_write_duplicate_groups_rejects_nul_path`, `test_parse_duplicate_group_file_rejects_carriage_return`, `test_parse_duplicate_group_file_rejects_invalid_utf8`, and `test_parse_duplicate_group_file_rejects_nul_path`; empty group serialization writes no phantom newline, covered by `test_write_duplicate_groups_empty_input`; nonempty groups round-trip through serialization and parsing using literal LF separators, including UTF-8 paths, all former `splitlines()` separator characters, and leading/trailing spaces, covered by `test_write_duplicate_groups_round_trip`; zero-byte audit sizes and estimates are `0.0 B` and `0.0-0.0 minutes at 20-80 MiB/s`, while 80 MiB estimates are `0.0-0.1 minutes at 20-80 MiB/s` and 1 GiB estimates are `0.2-0.9 minutes at 20-80 MiB/s`, covered by `test_human_bytes_zero`, `test_time_range_zero_bytes`, and `test_time_range_representative_bytes` and `test_time_range_large_bytes`; the pre-KiB, KiB, MiB, GiB, TiB, and PiB boundaries format as `1023.0 B`, `1.0 KiB`, `1.0 MiB`, `1.0 GiB`, `1.0 TiB`, and `1.0 PiB`, covered by `test_human_bytes_before_kib_boundary`, `test_human_bytes_mib_boundary`, and `test_human_bytes_gib_boundary`, `test_human_bytes_kib_boundary`, `test_human_bytes_tib_boundary`, and `test_human_bytes_pib_boundary`; audit duplicates with conflicting sizes are rejected, and direct helper tests cover both scanners; audit stat records require a separator, nonempty path, and nonnegative decimal size; `human_bytes` and `time_range` reject negative totals, covered by `test_audit_size_helpers_reject_negative`; preserving filenames containing newlines under the documented UTF-8 assumption; discovery roots are centrally shell-quoted, including ledger-update callers, empty trees are safe, and shared exclusions prune `@eaDir`, `.objects`, and ledgers consistently. Local tests cover their command properties; non-UTF-8 names remain pending Oxygen acceptance; malformed remote discovery UTF-8 is rejected before path parsing; shared SSH decoding rejects malformed UTF-8 before audit stat parsing with the stable `remote SSH output is not UTF-8` diagnostic; discovery uses its distinct `remote discovery output is not UTF-8` diagnostic, while containment escapes retain their own path-specific errors; this decode-versus-containment boundary is covered by transport tests.
- Empty review stages are full no-ops: `copy_stage` invokes no remote mkdir, tar, or chmod subprocesses; this includes empty year directories and stages containing only symlinks. Empty remote discovery returns cleanly, and ledger updates skip hashing when there are no new paths; the primary ledger remains unchanged while an explicitly requested output ledger is still written.
- Review staging skips symlink source paths before creating hardlinks.
- Review staging also requires each resolved source to remain beneath `source_root`, blocking symlinked-parent traversal.
- Review staging rejects symlinked stage roots and year directories before hardlink creation.
- Object storage rejects symlinked roots, `sha256` stores before migration writes, parent directories, and shard directories; migration rejects symlinked visible paths and their parents plus symlinked object-path parents before publication, covered by `test_process_record_rejects_symlink_destination`, `test_process_record_rejects_symlink_destination_parent`, and `test_migrate_rejects_symlink_object_shard`; verification reports shard links without traversal, and GC discovery/summary abort on malformed object names, symlinked shards, and object files, covered by `test_verify_objects_reports_symlink_shard`, `test_gc_rejects_malformed_object_file`, `test_gc_summary_rejects_malformed_object_file`, `test_gc_rejects_symlink_object_shard`, `test_gc_rejects_symlink_object_file`, and `test_gc_summary_rejects_symlink_object_shard`; standalone verification and GC discovery/summary reject the same redirected storage, including symlinked stores covered by `test_gc_rejects_symlink_object_store` and `test_gc_summary_rejects_symlink_object_store`; selective verification requires nonempty relative paths, rejects NUL input and traversal escapes, and rejects symlink selections and symlinked selection parents before resolving them, covered by `test_verify_selected_rejects_nul_path`, `test_verify_selected_rejects_empty_path`, `test_verify_selected_rejects_path_escape`, `test_verify_selected_rejects_absolute_path`, `test_migrate_rejects_symlink_object_store`, `test_verify_objects_rejects_symlink_store`, `test_verify_selected_rejects_symlink`, `test_verify_selected_rejects_symlink_parent`, `test_migrate_rejects_symlink_object_root_parent`, `test_verify_objects_rejects_symlink_root_parent`, `test_gc_rejects_symlink_object_root_parent`, and `test_gc_summary_rejects_symlink_object_root_parent`; apply rejects symlinked manifest inputs and manifest parents, normalizes malformed manifest UTF-8/JSON, rejects non-object manifest roots, non-string, empty, or relative root/object paths (covered by `test_migrate_rejects_relative_manifest_root` and `test_migrate_rejects_relative_manifest_object_root`), malformed record field types, invalid digests, negative metadata, strict integer metadata/version fields (rejecting JSON booleans), nonempty relative record paths (including rejection of whitespace-only values), and duplicate reviewed paths, and rejects reviewed paths and reviewed path parents before processing, covered by `test_migrate_rejects_empty_manifest_record_path`, `test_migrate_rejects_absolute_manifest_record_path`, `test_migrate_rejects_duplicate_manifest_paths`, `test_migrate_rejects_boolean_manifest_version`, `test_migrate_rejects_boolean_manifest_metadata`, `test_migrate_rejects_negative_manifest_metadata`, `test_migrate_rejects_relative_manifest_object_root`, `test_migrate_rejects_empty_manifest_object_root`, `test_migrate_rejects_invalid_manifest_digest`, `test_migrate_rejects_non_string_manifest_record_path`, `test_migrate_rejects_non_string_manifest_root`, `test_migrate_rejects_non_object_apply_manifest`, `test_migrate_rejects_invalid_utf8_apply_manifest`, `test_migrate_rejects_invalid_json_apply_manifest`, `test_migrate_rejects_symlink_apply_manifest`, `test_migrate_rejects_symlink_apply_manifest_parent`, `test_migrate_apply_manifest_rejects_symlink_path`, and `test_migrate_apply_manifest_rejects_symlink_parent`; missing reviewed files are reported without creating the object store, covered by `test_migrate_cli_rejects_missing_manifest_record_file`.

## Current status

Local implementation review has no significant automated findings. Direct migration and verification APIs reject negative limits as `limit must not be negative` and boolean limits as `limit must be a nonnegative integer`, covered by `test_migrate_rejects_negative_limit`, `test_verify_rejects_negative_limit`, `test_migrate_rejects_boolean_limit`, and `test_verify_rejects_boolean_limit`. `visible_files` validates root, selection, exclude container/pattern, excluded-root type/directory/parents, progress, and verbose types before traversal; a disappearing exclusion cannot redirect traversal because it is only compared as an exclusion target; missing and external excluded roots remain allowed because migration may create or use object stores outside the archive; excluded-root permission failures cannot redirect traversal, and `os.walk` permission errors propagate via its error callback; symlink directories are not followed, symlink files remain filtered even when wildcard patterns match, and surviving files are sorted after link filtering; reserved `.DS_Store`, lock, ledger, and object-store paths stay excluded at every depth, including nested `.objects` and `@eaDir` directories, using exact documented spellings; an overlapping excluded subtree is pruned before descendant inspection; wildcard patterns cannot re-enable reserved `.objects` or `@eaDir` directories, and cannot alter excluded-root pruning, excluding the discovery root itself returns no files; selected subdirectories also return no files when excluded directly or by an excluded ancestor; sibling names sharing a path prefix remain distinct, and relative inputs plus `.`/`..` components are resolved canonically before comparison; normalized missing selections fail before traversal; normalized missing excluded roots remain allowed for migration-created stores; external path case and Unicode normalization follow host filesystem semantics (no application case-folding or normalization), including selected directories and Unicode aliases; byte objects are rejected at the `Path` type boundary rather than decoded by the application, covered for both roots and selections by `test_visible_files_rejects_invalid_root_and_selection`, regardless of user patterns, covered by `test_visible_files_propagates_walk_errors`, `test_visible_files_does_not_follow_symlink_directories`, `test_visible_files_excludes_symlinks`, `test_visible_files_sorts_after_symlink_filtering`, and `test_visible_files_is_sorted_and_excludes_metadata`; nested metadata and index-directory coverage are included in that test; its wildcard assertion also confirms nested reserved names remain excluded; excluded-subtree overlap is covered by `test_visible_files_prunes_overlapping_excluded_subtree`, including its wildcard assertion, and root equality by `test_visible_files_excludes_discovery_root`; selected-subdirectory overlap is covered by `test_visible_files_excludes_selected_subdirectory`, with prefix separation covered by `test_visible_files_does_not_confuse_path_prefixes` and relative resolution by `test_visible_files_resolves_relative_paths`; parent-component normalization is covered by `test_visible_files_normalizes_parent_paths`, including redundant `.` components; missing normalized selections are covered by `test_visible_files_rejects_normalized_missing_selection`; normalized missing exclusions by `test_visible_files_accepts_normalized_missing_excluded_root`. its pattern-precedence coverage is included in the same test. byte-valued exclude patterns are rejected before traversal; string patterns containing NUL/newline are local `fnmatch` inputs and never serialized or parsed; duplicate, empty, and whitespace-only patterns are harmless; patterns are matched against basenames, not path strings; path-separator patterns therefore do not match nested basenames; Windows-style backslashes remain literal pattern characters; glob metacharacters cannot expand that basename scope; escaping, literal metacharacter, and case behavior remain standard host `fnmatch` semantics without cross-platform emulation; malformed-looking strings have no custom parser, while non-string patterns are rejected; pattern length/count adds only ordinary per-basename `fnmatch` work; patterns do not drive traversal or duplicate results; empty trees return an empty result regardless of exclude patterns; matching directory names prune descent, while matching file names filter only that file; excluded directory symlinks are not traversed or dereferenced; an excluded root returns no files before pattern matching, including when a selected file lies beneath it; trailing separators normalize to the same excluded-root identity; relative excluded roots use host `Path.resolve()` semantics and cannot broaden traversal; `..` aliases are canonicalized before exclusion checks, including selected-path aliases, parent aliases, `.` components, trailing separators, combined dot/separator aliases, parent/separator aliases, redundant separators, combined `..`/`.` aliases, and aliases beneath excluded roots, including selected-directory aliases, including parent aliases; trailing or redundant separators on excluded roots preserve selected-file exclusion, including aliased selected directories; missing selections fail before traversal; symlinked selections and symlinked selection parents are rejected before canonical traversal and selected-file filtering, including dangling links, including directory selections and symlinked selection parents; inaccessible selection components propagate filesystem errors; symlinked excluded-root components are rejected before resolution; dangling symlinked root ancestors are likewise rejected before target access; excluded-root lexical aliases with dangling components are rejected before `..` resolution; trailing separators do not hide excluded-root symlinks or symlinked parents, including dangling parents; `.` and `..` aliases remain canonicalized with those separators; dangling root symlinks retain the root-specific diagnostic; lexical aliases containing dangling symlink components are rejected before `..` resolution; missing excluded roots are resolved lexically and become authoritative if later created; permission errors during excluded-root validation propagate and fail closed; excluded-root matching is lexical and does not provide mount or device isolation, including for selected files; selected files apply reserved-name and exclude-pattern filters before return; symlink files are filtered by link name before final symlink removal, including matching exclude patterns, even when dangling, valid absolute and relative external exclusions preserve archive files, including selected directories; relative parent aliases, `.` aliases, combined dot/parent aliases, mixed alias/separator forms, trailing separators, redundant separators, and Unicode aliases, including combined Unicode/separator aliases, mixed Unicode parent aliases, mixed Unicode dot/parent aliases, Unicode parent-separator aliases, combined Unicode parent/separator aliases, nested Unicode lexical aliases, sibling Unicode excluded subtrees, multiple-sibling Unicode exclusions, reserved-sibling Unicode aliases, reserved-file-neighbor Unicode aliases, excluded-pattern-neighbor Unicode aliases, multiple excluded-pattern Unicode aliases, directory-pattern Unicode aliases, nested directory-pattern Unicode aliases, Unicode directory-pattern aliases, exact Unicode-pattern aliases, Unicode wildcard-pattern aliases, separator-spanning wildcard aliases, Unicode character-class aliases, negated Unicode character-class aliases, literal wildcard-character aliases, literal character-class bracket aliases, malformed Unicode character-class aliases, empty-pattern Unicode aliases, whitespace-pattern Unicode aliases, leading/trailing-space Unicode aliases, tab-containing Unicode aliases, newline-containing Unicode aliases, carriage-return Unicode aliases, form-feed Unicode aliases, vertical-tab Unicode aliases, mixed control-whitespace Unicode aliases, Unicode punctuation-pattern aliases, emoji-pattern Unicode aliases, combining-mark Unicode aliases, variation-selector Unicode aliases, zero-width Unicode aliases, bidirectional-control Unicode aliases, private-use Unicode aliases, noncharacter-pattern Unicode aliases, supplementary-plane Unicode aliases, mathematical-symbol Unicode aliases, superscript/subscript Unicode aliases, enclosed-alphanumeric Unicode aliases, currency-symbol Unicode aliases, letterlike-symbol Unicode aliases, geometric-shape Unicode aliases, dingbat Unicode aliases, musical-symbol Unicode aliases, weather-symbol Unicode aliases, transport-symbol Unicode aliases, map-symbol Unicode aliases, arrow-symbol Unicode aliases, decorative-symbol Unicode aliases, emoji-modifier Unicode aliases, emoji ZWJ-sequence Unicode aliases, flag-sequence Unicode aliases, tag-sequence Unicode aliases, Unicode redundant separators, Unicode dot components, Unicode combined dot/separator aliases, Unicode combined redundant aliases, and trailing Unicode separators, preserve selected-directory exclusion semantics, including aliased selected files and normalized absolute external paths, including `.` components and combined `..`/`.`/separator aliases; missing external exclusions preserve selected files, including selected directories, missing Unicode aliases, missing relative aliases, and missing relative parent aliases, including aliased selections with combined missing-path components; excluded-root permission failures propagate before selected-file filtering; symlinked external roots and their parents are likewise rejected for aliased selections, including selected directories, external-root parent aliases, and dangling parents, including dangling links; external-root permission errors propagate before filtering, including selected directories, including denied `is_symlink` checks, parent `.exists()` checks, and parent components; invalid external-root types are rejected first; file-valued external roots fail closed before selected-file filtering, including selected directories, and `.is_dir()` permission errors propagate; missing aliased external paths remain lexical exclusions; existing file-valued parent components are rejected before resolution; missing ancestors remain lexical; root, selected, and excluded-root `.resolve()` permission errors propagate; selected `.exists()` permission errors also propagate; selected `.is_file()` errors propagate after containment validation; selected-file metadata link checks also propagate; selected containment is checked before reserved-name or exclude-pattern filtering; selected directories remain traversal-scoped and are never returned as files; direct selected-directory symlinks and symlinked selected-directory parents are rejected before resolution; excluded selected directories return before traversal, including selected directories beneath external excluded roots and their normalized aliases, parent aliases, and trailing or redundant separators; selected directories matching exclusion patterns are pruned before traversal; selected `.objects` and `@eaDir` directories are also pruned; progress reporting cannot bypass selected-directory pruning; verbose progress is likewise silent for pruned selections; normalized selected-directory aliases, including parent aliases, remain subject to pruning; trailing or redundant separators cannot bypass selected-directory pruning; missing selected directories fail before traversal; selected-directory `.is_file()` errors propagate before traversal; ledger-name filtering remains authoritative after pattern checks; wildcard patterns cannot re-enable selected reserved metadata; inaccessible or missing selected parents cannot reach traversal or filtering; selected-parent `.is_symlink()` errors propagate; all symlinked selected parents and direct selected links are rejected before selected `.resolve()`; all symlinked root parents and root link-check errors are rejected before root `.resolve()`; root `.is_dir()` errors propagate after resolution; selected link-check errors propagate before resolution and cannot authorize traversal; symlink ancestors are rejected even when descendants are missing; relative selected paths resolve from the host working directory; redundant separators and `..` components are canonicalized before external exclusion checks, including external `.`, `..`, redundant-separator, and trailing-separator components, covered by `test_visible_files_accepts_external_excluded_root` and `test_visible_files_accepts_relative_external_excluded_root` (including trailing separators) and `test_visible_files_accepts_missing_external_excluded_root`, and rejects byte-valued excluded roots plus symlinked roots, selections, excluded roots, and parents, covered by `test_visible_files_rejects_invalid_exclude_container`, `test_visible_files_rejects_invalid_flags`, `test_visible_files_rejects_invalid_root_and_selection`, `test_visible_files_rejects_invalid_excluded_root`, `test_visible_files_rejects_file_excluded_root`, `test_visible_files_rejects_symlinked_excluded_root_parent`, `test_visible_files_rejects_non_string_exclude_pattern`, `test_visible_files_rejects_symlink_root`, `test_visible_files_rejects_symlink_selection`, `test_visible_files_rejects_symlink_selection_parent`, and `test_visible_files_rejects_symlink_excluded_root`. Combining emoji sequences remain literal basename patterns; coverage is in `test_visible_files_uses_combining_emoji_patterns_with_alias`. Emoji keycap sequences likewise remain literal; coverage is in `test_visible_files_uses_emoji_keycap_patterns_with_alias`. Emoji presentation selectors remain literal; coverage is in `test_visible_files_uses_text_emoji_presentation_patterns_with_alias`. Skin-tone plus ZWJ sequences likewise remain literal; coverage is in `test_visible_files_uses_skin_tone_zwj_patterns_with_alias`. Gendered emoji sequences likewise remain literal; coverage is in `test_visible_files_uses_gendered_emoji_patterns_with_alias`. Emoji hair-style sequences likewise remain literal; coverage is in `test_visible_files_uses_emoji_hair_style_patterns_with_alias`. Emoji family/role sequences likewise remain literal; coverage is in `test_visible_files_uses_emoji_family_role_patterns_with_alias`. Emoji couple sequences likewise remain literal; coverage is in `test_visible_files_uses_emoji_couple_patterns_with_alias`. Emoji family sequences likewise remain literal; coverage is in `test_visible_files_uses_emoji_family_patterns_with_alias`. Regional-indicator plus modifier sequences likewise remain literal; coverage is in `test_visible_files_uses_regional_modifier_patterns_with_alias`. Emoji combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_emoji_combining_mark_patterns_with_alias`. Emoji selector/modifier combinations likewise remain literal; coverage is in `test_visible_files_uses_emoji_selector_modifier_patterns_with_alias`. Emoji tag sequences likewise remain literal; coverage is in `test_visible_files_uses_emoji_tag_patterns_with_alias`. Digit-keycap variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_digit_keycap_patterns_with_alias`. Enclosed-alphanumeric sequences likewise remain literal; coverage is in `test_visible_files_uses_enclosed_alphanumeric_double_circle_patterns_with_alias`. Double-circle geometric symbols likewise remain literal; coverage is in `test_visible_files_uses_double_circle_patterns_with_alias`. Arrow symbols likewise remain literal; coverage is in `test_visible_files_uses_double_arrow_symbol_patterns_with_alias`. Black-diamond dingbats likewise remain literal; coverage is in `test_visible_files_uses_black_diamond_dingbat_patterns_with_alias`. G-clef supplementary-plane symbols likewise remain literal; coverage is in `test_visible_files_uses_g_clef_patterns_with_alias`. Supplementary noncharacters likewise remain literal where the host filesystem accepts them; coverage is in `test_visible_files_uses_supplementary_noncharacter_patterns_with_alias`, skipped when the filesystem rejects the filename. Supplementary private-use symbols likewise remain literal where accepted; coverage is in `test_visible_files_uses_supplementary_private_use_patterns_with_alias`, with the same filesystem limitation. Bidirectional-control symbols likewise remain literal; coverage is in `test_visible_files_uses_bidirectional_control_patterns_with_alias`. Vertical-tab/form-feed combinations likewise remain literal; coverage is in `test_visible_files_uses_vertical_form_control_patterns_with_alias`. Zero-width non-joiner symbols likewise remain literal; coverage is in `test_visible_files_uses_zero_width_non_joiner_patterns_with_alias`. Interrobang punctuation likewise remains literal; coverage is in `test_visible_files_uses_interrobang_patterns_with_alias`. Supplementary mathematical-pi symbols likewise remain literal; coverage is in `test_visible_files_uses_supplementary_math_pi_patterns_with_alias`. Eight-spoked decorative-star symbols likewise remain literal; coverage is in `test_visible_files_uses_decorative_star_patterns_with_alias`. Paired circle/diamond shapes likewise remain literal; coverage is in `test_visible_files_uses_paired_shape_patterns_with_alias`. Gear-plus-variation-selector symbols likewise remain literal; coverage is in `test_visible_files_uses_gear_variation_patterns_with_alias`. Symbol-plus-combining-acute sequences likewise remain literal; coverage is in `test_visible_files_uses_symbol_combining_mark_patterns_with_alias`. Shape-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_shape_variation_patterns_with_alias`. Shape-plus-combining-mark-plus-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_shape_combining_selector_patterns_with_alias`. Selector-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_selector_multiple_combining_patterns_with_alias`. Tag-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_tag_variation_patterns_with_alias`. Keycap-plus-combining-acute sequences likewise remain literal; coverage is in `test_visible_files_uses_keycap_combining_patterns_with_alias`. Flag-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_flag_multiple_combining_patterns_with_alias`. Tag-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_tag_multiple_combining_patterns_with_alias`. Flag-plus-ZWJ-plus-combining-acute sequences likewise remain literal; coverage is in `test_visible_files_uses_flag_zwj_combining_patterns_with_alias`. Family-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_family_multiple_combining_patterns_with_alias`. Flag-plus-tag-plus-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_flag_tag_combining_patterns_with_alias`. Flag-plus-ZWJ-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_flag_zwj_tag_patterns_with_alias`. Flag-plus-ZWJ-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_flag_zwj_multiple_combining_patterns_with_alias`. Couple-plus-ZWJ-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_couple_zwj_variation_patterns_with_alias`. Family-plus-ZWJ-plus-multiple-combining-mark-plus-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_family_zwj_combining_selector_patterns_with_alias`. Family-plus-modifier-plus-ZWJ-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_family_modifier_zwj_tag_patterns_with_alias`. Role-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_role_variation_patterns_with_alias`. Role-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_role_multiple_combining_patterns_with_alias`. Role-plus-ZWJ-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_role_zwj_variation_patterns_with_alias`. Role-plus-modifier-plus-ZWJ-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_role_modifier_zwj_combining_patterns_with_alias`. Role-plus-modifier-plus-ZWJ-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_role_modifier_zwj_variation_patterns_with_alias`. Role-plus-modifier-plus-ZWJ-plus-multiple-combining-mark-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_role_modifier_zwj_combining_variation_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_variation_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_combining_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_tag_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-multiple-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_tags_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-combining-mark-plus-multiple-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_combining_tags_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-variation-selector-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_variation_tags_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-combining-mark-plus-variation-selector-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_combining_variation_tags_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-multiple-combining-mark-plus-variation-selector-plus-multiple-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_combining_variation_tags_multiple_patterns_with_alias`. Role-plus-two-modifier-plus-ZWJ-plus-combining-mark-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_role_two_modifiers_zwj_combining_variation_patterns_with_alias`. Role-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_role_tag_patterns_with_alias`. Flag-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_flag_tag_patterns_with_alias`. Couple-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_couple_variation_patterns_with_alias`. Family-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_family_variation_patterns_with_alias`. Flag-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_flag_variation_patterns_with_alias`. Modifier-plus-combining-acute sequences likewise remain literal; coverage is in `test_visible_files_uses_modifier_combining_patterns_with_alias`. ZWJ-plus-variation-selector sequences likewise remain literal; coverage is in `test_visible_files_uses_zwj_variation_patterns_with_alias`. ZWJ-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_zwj_multiple_combining_patterns_with_alias`. Modifier-plus-ZWJ-plus-combining-acute sequences likewise remain literal; coverage is in `test_visible_files_uses_modifier_zwj_combining_patterns_with_alias`. Flag-plus-ZWJ sequences likewise remain literal; coverage is in `test_visible_files_uses_flag_zwj_patterns_with_alias`. ZWJ-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_zwj_tag_patterns_with_alias`. Modifier-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_modifier_tag_patterns_with_alias`. Family-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_family_tag_patterns_with_alias`. Couple-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_couple_tag_patterns_with_alias`. Family-plus-ZWJ-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_family_zwj_tag_patterns_with_alias`. Modifier-plus-ZWJ-plus-tag sequences likewise remain literal; coverage is in `test_visible_files_uses_modifier_zwj_tag_patterns_with_alias`. Symbol-plus-multiple-combining-mark sequences likewise remain literal; coverage is in `test_visible_files_uses_multiple_combining_patterns_with_alias`. Punctuation-plus-combining-acute sequences likewise remain literal; coverage is in `test_visible_files_uses_punctuation_combining_patterns_with_alias`. Currency-plus-combining-acute sequences likewise remain literal; coverage is in `test_visible_files_uses_currency_combining_patterns_with_alias`. Letterlike-symbol-plus-combining-acute sequences likewise remain literal; coverage is in `test_visible_files_uses_letterlike_combining_patterns_with_alias`. Snowman weather symbols likewise remain literal; coverage is in `test_visible_files_uses_snowman_patterns_with_alias`. Transport symbols likewise remain literal; coverage is in `test_visible_files_uses_transport_symbol_patterns_with_alias`. Compass map symbols likewise remain literal; coverage is in `test_visible_files_uses_compass_map_patterns_with_alias`. Bitcoin-sign currency symbols likewise remain literal; coverage is in `test_visible_files_uses_bitcoin_currency_patterns_with_alias`. Prescription-sign letterlike symbols likewise remain literal; coverage is in `test_visible_files_uses_prescription_sign_patterns_with_alias`. Subscript-nine symbols likewise remain literal; coverage is in `test_visible_files_uses_subscript_nine_patterns_with_alias`.

Production status remains pending the Oxygen/Synology checks below.

## Required on Oxygen before production

For a representative year, review the dry-run manifest before applying it:

```sh
oxygen-migrate 2006 --dry-run --limit 10 --manifest trial.json
# inspect/delete or reject trial.json as appropriate
oxygen-migrate --apply trial.json
oxygen-verify /var/services/photo/.objects --verbose
```

- [ ] Run a small real-year migration and rerun `oxygen-verify`.
- [ ] Confirm object and visible hardlink counts before and after.
- [ ] If `.objects` is external, run GC with explicit `--root` and verify lock coordination.
- [ ] Test Synology indexing and confirm `.objects` is excluded.
- [ ] Test Pixette/WebDAV open, rename, and write behavior with the real account.
- [ ] Record permissions, ACLs, ownership, timestamps, and xattrs before/after.
- [ ] Confirm the no-in-place-edit policy and keep the ledger until acceptance.
- [x] Run `make -n archive-reviewed` and confirm copy precedes ledger update/install.
- [ ] After `make archive-reviewed`, verify copied files are present in the installed remote ledger.
- [ ] Confirm RAID-0 is not treated as backup and record the separate backup plan.

- Repository path scan found no `/volume1/photo` references; canonical spelling remains `/var/services/photo`.

No production migration is approved by local tests alone; `tests/test_cli.py` guards this warning in the documentation status checks; README and this review both state that local tests do not approve production migration; `tests/test_cli.py` checks both source documents and the warning.
